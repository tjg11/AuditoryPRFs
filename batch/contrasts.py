import os
import json
from utils import get_htmls
from dotenv import load_dotenv

def get_contrasts():
    """Uses environment variables to create and save contrast maps for each
    subject. Returns the number of maps created. NOT FINISHED"""

    # load environment variables
    load_dotenv()

    # get subject list and data paths


    # parameters: sub_id, num_runs, base_path, 
    get_htmls()
